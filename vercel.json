{
  "buildCommand": "prisma generate --no-engine && prisma migrate deploy && (psql $POSTGRES_URL -f scripts/create-enphase-table.sql 2>/dev/null || true) && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"]
}
